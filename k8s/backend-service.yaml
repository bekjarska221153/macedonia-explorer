apiVersion: v1
kind: Service
metadata:
  name: macedonia-explorer-backend
  namespace: macedonia-explorer
spec:
  selector:
    app: macedonia-explorer-backend
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
